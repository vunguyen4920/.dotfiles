(defcfg
  concurrent-tap-hold yes ;; Important: Must be enabled for chords v2
  process-unmapped-keys yes
)

(defsrc
  a   s   d   f   j   k   l   ;
)

(deflayermap (base-layer)
  caps esc)

(defchordsv2
  (f d) lctl 37 first-release ()
  (d s) lalt 37 first-release ()
  (f s) lmet 37 first-release ()

  (j k) rctl 37 first-release ()
  (k l) ralt 37 first-release ()
  (j l) rmet 37 first-release ()
)

(deflayer base
  a   s   d   f   j   k   l   ;
)
